{
  "framework": "vite",
  "buildCommand": "pnpm --filter . run build",
  "outputDirectory": "dist",
  "installCommand": "npm i -g pnpm@11.1.3 && PATH=$(npm config get prefix)/bin:$PATH && git clone https://github.com/petr0n/aburungo-design-system.git ../aburungo-design-system && (cd ../aburungo-design-system&&git checkout da36cf524a181fef80e40d7c7d779bc63fbd5ad8&&pnpm i&&pnpm build) && pnpm i --frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
