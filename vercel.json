{
  "buildCommand": "pnpm --filter . run build",
  "outputDirectory": "dist",
  "installCommand": "npm install -g pnpm@11.1.3 && export PATH=\"$(npm bin -g):$PATH\" && git clone https://github.com/petr0n/aburungo-design-system.git ../aburungo-design-system && cd ../aburungo-design-system && pnpm install --frozen-lockfile && pnpm build && cd - && pnpm install --frozen-lockfile"
}
